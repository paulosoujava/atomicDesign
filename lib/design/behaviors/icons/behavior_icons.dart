import 'package:atom/design/atom/icons/atom_icon.dart';
import 'package:flutter/material.dart';

class BehaviorIcons {
  static email() => AtomIcon(
        Icons.email,
        semanticLabel: "test",
        color: Colors.red,
      );
  static lock() => AtomIcon(
        Icons.lock,
        semanticLabel: "test",
        color: Colors.red,
      );
  static back() => AtomIcon(
        Icons.arrow_back,
        semanticLabel: "test",
        color: Colors.black,
      );
}
